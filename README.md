# 共享的话题清单：由话题总结agent根据每轮的query进行添加，每个话题设置一个计数器，当这个话题多次(暂定5轮，之后根据不同层级的话题如人物、音乐等做不同的计数器)，当同类的主体类型被选中时，计数器-2，否则-1，被选中的具体主体计数器恢复计时器清零后放入待处理主体数组中，当数组达到一定大小后，根据每个主体对存储的QA对进行分类，并结合分类后的QA生成一个summary,并存入长期记忆中，需要加一个新的信息tag即总结时的主体(首先将上一轮的清单发给小模型，让其根据清单中的主体先替换掉代词，然后从清单中，选出此次查询的可能涉及的主体，然后计数器操作由外部处理)
## [清单内容(暂定)：
 * 顶级(格式{搜索关键词：提取后的有效信息})(不做计数器，防止重复搜索,滑动窗口的搜索缓存)
 * 一级(格式{"对应活跃主体类型(如人物、音乐等)":[{对应活跃类型的具体主体:计数器},{对应活跃类型的具体主体:计数器}]})]...
# 记忆分层调度：不再短期记忆和长期记忆一起放入，只有当短期记忆无法获取所需消息，且判断为需要长期记忆再调用.让大模型去思考，先让大模型判断是否需要记忆，如果需要根据短期记忆看看能不能直接回答，如果不能就调用长期记忆，返回值为
{
  "route": "None | short | long",
  "payload": {
    // 若为 None: 返回空对象 {}
    // 若为 short: 返回从原始 QA 数组中筛选出的、与当前意图直接相关的 QA 对象数组
    "context_qa": [{"q": "...", "a": "..."}], 
    // 若为 long: 返回两个数组：1. 当前意图的 Summary 提取；2. 需要查询的主体关键词
    "query_summary": ["用户想了解..."],
    "search_subjects": ["主体1", "主体2"]
  }
}
# 长期记忆查询：tag文本顾虑查询和rag并行，文本过滤通过主体检索，rag通过summary检索